# Locales generated into the image. locale-gen runs once at build time.
#
# Adding one needs the locale source data restored first: usr/share/i18n is in
# NoExtract, so locale-gen prints "Generation complete" and generates nothing.
# See examples/04-add-a-locale.sh.
en_US.UTF-8 UTF-8
